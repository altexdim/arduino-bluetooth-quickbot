#ifndef TYPES_H
#define TYPES_H

enum WHEEL {
    WHEEL_LEFT,
    WHEEL_RIGHT,
    WHEEL_COUNT
};

enum WHEEL_PINS {
    WHEEL_PIN_EN1,
    WHEEL_PIN_EN2,
    WHEEL_PIN_PWM,
    WHEEL_PIN_COUNT
};

#endif
